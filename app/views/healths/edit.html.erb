<%# --- ヘッダー呼び出し ----------------------------------------------- %>
<%= render "shared/header"%>

<%# --- 体調入力フォーム --------------------------------------------------- %>
<%= form_with(url: health_path, model: @health, local: true, method: :patch) do |f| %>
  <%= render "form", f: f %>
<% end %>

<%# --- フッター呼び出し ----------------------------------------------- %>
<%= render "shared/footer"%>

